export interface Product {
    id: number;
    img: string;
    price: number;
    title: string;
    name: string;
    description: string;
}

export interface Country {
    code: string;
    name: string;

}